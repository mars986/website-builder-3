{
	"name": "website-builder-3",
	"private": true,
	"description": "Build your own website hosting platform with Workers and D1.",
	"license": "Apache-2.0",
	"engines": {
		"node": ">=18"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.5.30",
		"@cloudflare/workers-types": "^4.20241205.0",
		"typescript": "^5.0.0",
		"vitest": "^2.1.8",
		"wrangler": "^3.99.0"
	},
	"scripts": {
		"dev": "wrangler dev --remote",
		"build": "echo \"No build required\"",
		"deploy": "wrangler deploy",
		"setup": "node scripts/setup.js",
		"setup:quick": "node scripts/setup-quick.js",
		"test": "vitest run",
		"test:watch": "vitest"
	},
	"dependencies": {
		"hono": "^4.2.1",
		"workers-qb": "^0.1.1"
	}
}
